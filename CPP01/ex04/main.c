#include <iostream>
#include <fstream>

int main(int ac, char *av[])
{
    if (ac == 4)
    {
        fstream MyFile(av[1]);
        MyFile.
    }
    else
        std::cout << "wrong input try again !" << std::endl;
}